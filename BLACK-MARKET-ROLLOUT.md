# LINSA 암시장 BM2

대상은 실제 플레이 사이트 `dkdrlahWl/linsa-rpg`입니다. 다른 게임 저장소의 HTML로 교체하지 않습니다.

## 운영 서버 적용 상태

2026-09-10 사용자가 Supabase SQL Editor에서 BM2 설치 SQL을 직접 실행했습니다. 이후 관리 연결로 다음 상태를 확인했습니다.

- `public.ringu_black_market(text,text,integer,uuid)` 함수와 공통 진열·구매 제한 테이블이 존재합니다.
- 기존 경제 서버 `economy_ready`는 true이며, 장비 목록 280종과 확률 `[80,15,4.9,0.1]`이 설치되어 있습니다.
- 서버 생성 함수의 가격표 및 구매 테이블의 가격 제약은 `[5,15,50,100]`입니다.
- `(account_id,rotation_id,slot)` 기본키와 `(account_id,request_id)` 고유키가 설치되어 있습니다.
- 로그인된 역할만 공개 암시장 함수를 실행할 수 있고, 익명 실행 및 로그인된 역할의 공통 진열 테이블 직접 조회 권한은 없습니다.
- 운영 서버의 공통 진열 생성 함수를 호출하여 현재 주기의 장비 5개와 고정된 옵션·가격을 반환받았습니다. 기존 유저의 재화·장비를 변경하거나 구매하지 않았습니다.

추가 PostgREST 스키마 캐시 갱신 알림 요청은 도구 보안 판정에서 차단되어 실행 여부를 확인하지 못했습니다. 설치 자체와 공통 진열 생성은 그 전에 별도 조회로 확인했습니다. 실제 로그인 계정의 브라우저 구매·재접속은 아직 검증하지 않았습니다.

## 규칙

한국시간 매일 00시와 18시에 진열이 바뀝니다. 00시~18시는 18시간, 18시~자정은 6시간입니다. 서버가 해당 주기 최초 요청에서 장비 5개를 추첨하여 저장합니다. 모든 계정의 이름·부위·등급·공격력·옵션·가격이 같습니다. 정각 이후 신규 요청은 이전 주기 상품을 구매할 수 없습니다.

일반 80%, 희귀 15%, 레어 4.9%, 에픽 0.1%로 기존 소환 9레벨 확률을 사용하지만, 화면 제목은 `등급별 진열 확률`로 표시합니다. 가격은 각각 정수 5/15/50/100개입니다. 각 부위는 균등 추첨하고 같은 부위·등급의 기존 장비를 균등 추첨하며 한 진열의 동일 기본 장비 중복은 제외합니다. 일반만 5개일 수도 있습니다. 구매는 표시된 장비를 확정적으로 받는 거래입니다.

계정당 주기별 상품마다 1회 구매할 수 있습니다. 5종을 각각 한 개씩 살 수 있으며 다른 계정의 구매 기회에는 영향을 주지 않습니다. 판매·분해·재접속 후에도 제한이 유지됩니다. 다음 주기에 새 상품별 구매 기회가 생깁니다.

## 서버 처리

기존 최신 세션 검사와 경제·경매장 잠금 순서를 사용합니다. 가격·시간·계정·장비 속성을 클라이언트에서 받지 않습니다. 정수 차감, 진열과 동일한 속성의 신규 장비 지급, 계정 revision 증가, 구매 제한 기록, 요청 영수증을 한 트랜잭션으로 처리합니다. 같은 요청 ID의 재시도는 영수증만 반환하고, 새로운 요청 ID도 계정/주기/상품 고유키로 재구매를 차단합니다.

## 화면 및 검증 범위

프런트엔드는 상점 또는 메뉴의 암시장 버튼으로 열며, 하단에 `암시장 BM2`가 표시됩니다. 성민 이름·대사·이미지와 기존 남청색·황동색 UI, 구매 확인창은 유지합니다. 이번 BM2 변경은 가격 안내·확률 제목·버전 표시와 대응 서버 SQL·테스트에 한정합니다.

GitHub Actions 실행 `34436481154`에서 BM2 변경 파일 해시, JavaScript 문법, 격리 데이터베이스의 가격 차감·재구매 제한 검사 및 기존 회귀 검사가 성공한 것을 확인했습니다. 이번 설치 확인 과정에서는 실제 휴대폰 또는 운영 계정으로 구매하지 않았습니다. 전체 기능의 실사용 검증이 끝났다고 간주하지 않습니다.
