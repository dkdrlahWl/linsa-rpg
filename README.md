# Ringu RPG

GitHub Pages frontend; Supabase Auth/Postgres backend. No PC server is required.

Only public assets and the publishable API key are included. Account data and credentials are NOT committed.
